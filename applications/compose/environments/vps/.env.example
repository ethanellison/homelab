# VPS Production Environment Variables

# Network Configuration
HOST_IP=0.0.0.0
DOMAIN=example.com
LAN_GATEWAY=192.168.1.1
LAN_NETWORK=192.168.1.0/24

# PiHole Configuration
PIHOLE_PASSWORD=your-secure-password
FTLCONF_LOCAL_IPV4=${HOST_IP}

# Redis Security
REDIS_PASSWORD=your-secure-redis-password

# Paperless Configuration
PAPERLESS_SECRET_KEY=your-secure-secret-key
PAPERLESS_ADMIN_USER=admin
PAPERLESS_ADMIN_PASSWORD=your-secure-admin-password
PAPERLESS_OCR_LANGUAGES=eng

# Paths
DATA_PATH=/opt

# Cloudflare Tunnel
CLOUDFLARE_TUNNEL_TOKEN=your-cloudflare-tunnel-token

# Tailscale (used by automation stack)
TS_AUTHKEY=your-tailscale-authkey
TS_CERT_DOMAIN=your-machine.ts.net

# Pocket ID (OIDC Provider)
# PUBLIC_APP_URL base domain for the Pocket ID instance (e.g. auth.example.com)
AUTH_DOMAIN=auth.example.com
# Encryption key (must be at least 16 characters long)
POCKET_ID_ENCRYPTION_KEY=your-secure-pocket-id-encryption-key-at-least-16-chars

# Actual Budget OIDC
# Discovery URL: https://<AUTH_DOMAIN>/.well-known/openid-configuration
ACTUAL_OPENID_DISCOVERY_URL=https://auth.example.com/.well-known/openid-configuration
ACTUAL_OPENID_CLIENT_ID=actual-budget
ACTUAL_OPENID_CLIENT_SECRET=generate-a-strong-secret
# Public URL of the Actual Budget instance (used for OIDC redirect URI)
ACTUAL_OPENID_SERVER_HOSTNAME=https://budget.example.com